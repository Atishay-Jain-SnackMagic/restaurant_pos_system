<% content_for :title do %>Change Inventory Quantity<% end %>

<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl text-center">Change Inventory Quantity</h1>

  <%= render partial: "form", locals: { inventory_unit: @inventory_unit, inventory_location: @inventory_location, submit_url: admin_inventory_location_inventory_units_path(@inventory_location) } %>

  <p class="mt-6 text-center">
    <%= link_to t(".back_to_inventories"), admin_inventory_locations_path, class: "inline-block bg-blue-600 text-white w-full py-2 rounded-md font-medium hover:bg-blue-700 transition" %>
  </p>
</div>
