<% if flash[:notice].present? %>
  <div class="mb-6">
    <div class="bg-green-100 border border-green-300 text-green-800 px-4 py-3 rounded-lg shadow">
      <%= flash[:notice] %>
    </div>
  </div>
<% end %>

<% if flash[:error].present? %>
  <div class="mb-6">
    <div class="bg-red-100 border border-red-300 text-green-800 px-4 py-3 rounded-lg shadow">
      <%= flash[:error] %>
    </div>
  </div>
<% end %>